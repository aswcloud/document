version: '3.1'
services:
  mongo:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
  
  server-comm:
    image: aoikazto/aswcloud-server
    restart: always
    ports:  
      - 8080:8088
